/target
**/*.rs.bk
*.orig
.DS_Store
# headless render / device screenshot scratch
/out.png
/screen.png
device-facts.out
# python / training artifacts
__pycache__/
*.pyc
/train/dataset*/
/train/detexify_raw/
# Experiment models (A/B runs). Committed on purpose: model.iwt (v1, SHIPPED — the corpus
# reference) and model_v2.* (Detexify+HWRT — the expression path's model + labels + counts).
# Everything else is a rejected or intermediate experiment.
/train/model_small.*
/train/model_full.*
/train/model_v3.*
# NOTE: train/model.iwt + train/model.labels.txt ARE committed — the reference
# model for the tests/corpus regression suite (small; also lets `make recognize`
# work straight from a clone).
# NOTE: Cargo.lock IS committed — this workspace produces binaries.

# local agent config — not part of the published project
/CLAUDE.md
/.claude/
